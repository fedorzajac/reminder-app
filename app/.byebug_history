continue
exit
rem.update_column(:due, true)
rem.update_column(due: true)
rem.errors
rem.update(due: true)
rem.update(due: false)
rem
rem.due = true
rem.due
rem
rem  = Reminder.find(self.id)
Reminder.find(self.id)
Reminder.find(self.id).due = false
Reminder.find(self.id).update(due: false)
Reminder.find(self.id)
rem
exit
Reminder.find(self.id).update(due: true)
Reminder.find(self.id)
p self.id
pp rem
